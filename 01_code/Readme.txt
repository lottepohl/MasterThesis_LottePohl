In this folder, different kinds of R scripts can be found, categorised into different sub-folders.

In the `save_data` folder, external data such as marine boundaries or environmental data are loaded and saved into the `03_data` folder.
In the `load_data` folder, these external data are loaded into RStudio from the `03_data` folder.